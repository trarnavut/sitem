<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Management" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <TextBlock Text="Name:" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="TxtName" Width="150" Margin="5"/>
            <TextBlock Text="Email:" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="TxtEmail" Width="150" Margin="5"/>
            <TextBlock Text="Phone:" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="TxtPhone" Width="150" Margin="5"/>

            <Button Content="Add" Width="80" Margin="5" Click="AddUser_Click"/>
            <Button Content="Update" Width="80" Margin="5" Click="UpdateUser_Click"/>
            <Button Content="Delete" Width="80" Margin="5" Click="DeleteUser_Click"/>
        </StackPanel>

        <DataGrid x:Name="UserDataGrid" Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding Users}" 
                  SelectionMode="Single" SelectionUnit="FullRow" Margin="0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
