<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BHealthyApp"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="BHealthyApp.BMIActivitiesWindow"
        Title="BMI &amp; Activities" SizeToContent="WidthAndHeight" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" av:DesignWidth="566">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Calculate BMI" Grid.Column="0" Grid.Row="0" Margin="5">
            <local:BMIUserControl x:Name="bmiControl" />
        </GroupBox>

        <GroupBox Header="Select Physical Activities" Grid.Column="1" Grid.Row="0" Margin="5">
            <ListBox x:Name="lstActivities" SelectionMode="Multiple" MinWidth="150" Height="Auto">
                <ListBoxItem Content="Jogging"/>
                <ListBoxItem Content="Running"/>
                <ListBoxItem Content="Swimming"/>
                <ListBoxItem Content="Play Tennis"/>
                <ListBoxItem Content="Play Basketball"/>
            </ListBox>
        </GroupBox>

        <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5"/>
        <Button x:Name="btnResult_Kopyala" Content="Result" Click="btnResult_Click" Padding="10" MinWidth="100" RenderTransformOrigin="1.905,-3.11" Grid.Column="1" Margin="245,25,-176,137"/>
        <Button x:Name="btnVisitWeb" Content="Visit Be Healthy Web" Click="btnVisitWeb_Click" Padding="10" MinWidth="150" Margin="245,74,-176,84" Grid.Column="1"/>
        <Button x:Name="btnBackToMenu" Content="Back to Menu" Click="btnBackToMenu_Click" Padding="10" MinWidth="100" Margin="245,127,-176,31" Grid.Column="1"/>
    </Grid>
</Window>